This pom file will compile the project into TeachHub/target/TeachHub.jar when used. 
However all dependencies will be stored in their respective JAR files in the TeachHub/target/dependency-jars folder.

After changing this POM out with the main one, use following command to run:
mvn package
