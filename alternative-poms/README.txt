change out the pom file in the main directory with one of these for slightly different compiling function.
the default POM currently only holds the dependencies, but must be manually compiled into any for of JAR

Each alternative POM comes with a README.txt file.